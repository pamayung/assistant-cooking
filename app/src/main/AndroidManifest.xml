<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.labs.wildhan.assistantcooking">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".AppController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_koki"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DetailActivity"
            android:label="@string/title_activity_detail"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ListBahan" />
        <activity android:name=".StratActivity" />
        <activity android:name=".Favorite" />
        <activity android:name=".TipsActivity" />
        <activity android:name=".About" />
        <activity android:name=".TipsVideo" />
        <activity
            android:name=".DetailFavorite"
            android:label="@string/title_activity_detail_favorite"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".StartFavorite" />
        <activity
            android:name=".KomentarActivity"
            android:label="@string/title_activity_komentar"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".TulisKomenActivity"></activity>
    </application>

</manifest>